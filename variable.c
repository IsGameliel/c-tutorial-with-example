#include <stdio.h>

int main(){
    // Variables in c
    int age = 12;
    // we declearde a variable called age and assign the value 12 to the variable.
    printf("%d\n", age);

    // reasigning variable to another variable 
    int newAge = age;
    printf("new age: %d\n", newAge);

    // Mulitple variable declearation
    int value1, value2 = 12;
    return 0;
}